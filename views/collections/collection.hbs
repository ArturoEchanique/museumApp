<div class="container collectionMain">


<h1>{{collection.title}}</h1>

<hr>

{{!-- {{#each artItems}}
is this
<a href="/collections/{{../collection.id}}/art/{{objectID}}"><img src="{{primaryImageSmall}}" alt=""></a>
{{/each}} --}}
<div class="container imagesGrid">
    <div class="row">
        {{#each artItems}}
        <div class="col-md-4">
            <a href="/collections/{{../collection.id}}/art/{{objectID}}"><img src="{{primaryImageSmall}}" alt=""></a>
        </div>
        {{/each}}
    </div>
</div>

</div>